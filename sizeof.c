#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
	/* the sizeof(variable) operator computes the size of a variable.And,
	 to print the result returned by sizeof we use %lu or %zu format specifier
	 %lu is used to format the output or  input of unsigned long integers.
	 %zu to print the variable of size_t length.z is a length modifier and u stands for unsigned type.
	 */
	/*
	printf("size of char = %lu\n ",sizeof(char));
	printf("size of int = %lu \n", sizeof(int));
	printf("size of float = %lu\n",sizeof(float));
	*/

}
